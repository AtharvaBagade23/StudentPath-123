{
  "version": 2,
  "buildCommand": "pnpm run build",
  "devCommand": "pnpm run dev",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "regions": ["bom1"],
  "env": {
    "DB_HOST": "required",
    "DB_PORT": "required",
    "DB_USER": "required",
    "DB_PASSWORD": "required",
    "DB_NAME": "required"
  }
}